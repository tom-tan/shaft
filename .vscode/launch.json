{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "gdb",
      "request": "launch",
      "preLaunchTask": "${defaultBuildTask}",
      "name": "Launch Program",
      "target": "${workspaceRoot}/shaft",
      "cwd": "${workspaceRoot}",
      "valuesFormatting": "parseText"
    }
  ]
}
